// Where students create the Notion internal connection whose token AutoSlides
// uses (Settings → Add-ons → Watch Notes) and grant it pages (Content access).
export const NOTION_CONNECTIONS_URL = 'https://app.notion.com/developers/connections';
