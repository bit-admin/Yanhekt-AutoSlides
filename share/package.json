{
  "name": "autoslides-share",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AutoSlides share viewer (v1) + AutoSlides Index (v2) — Cloudflare Worker, KV short links, D1 metadata.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build -c vite.apex.config.ts",
    "typecheck": "tsc --noEmit",
    "preview": "wrangler dev",
    "db:migrate": "wrangler d1 migrations apply autoslides-index-v2",
    "db:migrate:local": "wrangler d1 migrations apply autoslides-index-v2 --local",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "jspdf": "^4.2.1",
    "jszip": "^3.10.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "wrangler": "^4.128.0"
  },
  "overrides": {
    "sharp": "^0.35.3"
  }
}
