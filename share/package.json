{
  "name": "autoslides-share",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AutoSlides note share viewer — Cloudflare Worker (Static Assets) + KV short links.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.mjs",
    "typecheck": "tsc --noEmit",
    "preview": "wrangler dev",
    "deploy": "vite build && wrangler deploy"
  },
  "dependencies": {
    "jspdf": "^2.5.2",
    "jszip": "^3.10.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "wrangler": "^3.99.0"
  }
}
